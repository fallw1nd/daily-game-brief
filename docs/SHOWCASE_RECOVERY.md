# 发布会补齐

仅当持久状态的 `revisionRequest` 为 open、reason 为 `showcase_completion`，且已确认 packet 标记 `continuation.scope:showcase`、`preservePublished:true`，才消费已发布期次的自动补齐任务。普通人工修订仍使用既有授权入口。

- 从状态记录读取精确 packet blob，沿用原期次窗口；后来抓取的页面不能让窗口外公告进入旧期。
- 每个公告身份必须对应正文、简讯、已存在的相同事实或有来源支持的非实质内容理由。预算、热度和多游戏合集都不是实质公告排除理由。
- 已存在的同一事实填写 `existingEntryId`，但不能只凭游戏名或来源 URL 合并。不同事实新增简讯；地区独有平台、日期差异保留。
- 保留已有正文、人工修订、期标题、头条和发售日历。补齐提交不携带日历变更；英文仅补新条目，已有英文由程序保留。
- 未核验内容继续 needs_review。地区材料不全、清单有缺口或来源仅为 Highlights 时不宣称收录完整。
- GitHub 在已有状态分支事务中串行激活后续包；推送冲突须读取最新状态重新计算。编辑任务不直接操作状态队列。
- 2026-09-11 用户确认继续每天两次编辑调用。30/120/360 分钟是重试到期条件，由下一次既有运行消费，不承诺在该分钟立即完成。旧期补齐只更新目标归档及索引，不把首页回退到旧期。

## 实现验收状态（2026-09-10）

分批持久化、受限补齐状态、人工修订保护、标题规范化、地区缺口显示和名称缓存正在分支 `codex/showcase-completeness` 中实现，尚未上线。

真实官方 HTML 回放已发现并修正 Nintendo 导航误收、无链接卡片漏收、日文正文跨容器遗漏及 PlayStation 图片无障碍标题污染。当前解析结果是公告**片段**：日本 Nintendo 68、欧洲 Nintendo 42、PlayStation 34；这些数字不能替代独立的事实增量验收清单，也不能据此宣称完整覆盖。

尚待验收：自动发现活动的精确时间与地区归并、视频章节和独立总结回查、初次包 needs_review 的再采集、30/120/360 分钟重试与编辑任务唤醒、所有来源独立验收清单、真实名称查询及完整生产演练、本期修复与 Pages 上线。当前外部编辑任务每天两次调用，不能仅凭 GitHub 队列推进声称已实现半小时自动补齐。

视觉人工检查：桌面与 390px 下检查中英文清单标题、长游戏名、缺口说明、锚点唯一性；浅色/深色与各主题的焦点轮廓；Tab 访问清单链接后正确定位正文。遵循仓库约定，不使用 Windows 浏览器沙箱做视觉 QA。
