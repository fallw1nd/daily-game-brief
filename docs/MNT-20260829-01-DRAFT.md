# MNT-20260829-01 — verified 边界分钟丢失秒级精度

This temporary branch-local note exists so the fix PR can reference a concrete maintenance ID while the canonical entry is added to `docs/MAINTENANCE_LOG.md` before merge.

- **Discovered:** 2026-08-29
- **Priority:** P0
- **Area:** editorial handoff / time validation / fixed windows
- **Status:** in_progress
- **Evidence:** publication run 33236844373 built `2026-08-29-am` but `validate:data` rejected `2026-08-29-am-releases-2` as outside the fixed window. The selected source timestamp was `2026-08-28T09:00:11Z` (Beijing 17:00:11), which is inside the AM exclusive-start window, while the editorial/public `beijingTime` field retained only `2026-08-28 17:00`, collapsing the valid instant onto the excluded boundary.
- **Risk:** valid events in the first 59 seconds after an exclusive start can be falsely rejected; blindly allowing the boundary minute would instead admit exact-start events and events after an inclusive end cutoff.
- **Proposed resolution:** retain minute-level `beijingTime` for display, derive second-precision evidence only from selected opened sources, persist it as `timeEvidenceAt` for boundary-minute verified entries, and use the exact instant for validation. Boundary-minute evidence without second precision must fail closed for `verified` and may use `time_unverified`.
- **Close when:** regressions cover AM/PM exclusive starts, inclusive ends, minute-only evidence, and `time_unverified`; full Verify and Pages pass; no fixed window or historical production data is widened/rewritten.
- **Resolution:** pending.
- **Verification:** pending.
