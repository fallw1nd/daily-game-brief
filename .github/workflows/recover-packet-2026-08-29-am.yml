name: Recover packet 2026-08-29-am

on:
  push:
    branches:
      - automation/state
    paths:
      - .github/workflows/recover-packet-2026-08-29-am.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch exact packet recovery
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run news-discovery-shadow.yml --repo "$GITHUB_REPOSITORY" --ref main -f period=am -f edition=2026-08-29-am
