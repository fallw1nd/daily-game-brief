# requestedAt: 2026-08-28T09:00:04Z
name: Recover packet 2026-08-28-pm

on:
  push:
    branches:
      - automation/state
    paths:
      - .github/workflows/recover-packet-2026-08-28-pm.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch existing final packet workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run news-discovery-shadow.yml --repo "$GITHUB_REPOSITORY" --ref main -f period=pm
